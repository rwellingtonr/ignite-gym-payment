version: "3.8"

services:
  postgres:
    image: postgres-alpine
    restart: unless-stopped
    ports:
      - 5432:5432
    environment:
      - POSTGRESQL_USERNAME=postgres
      - POSTGRESQL_PASSWORD=Leardini
      - POSTGRESQL_DATABASE=ignite
